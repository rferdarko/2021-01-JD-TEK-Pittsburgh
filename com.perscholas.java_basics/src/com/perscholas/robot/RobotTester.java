package com.perscholas.robot;

public class RobotTester {
    public static void main(String[] args) {

        Robot myRobo = new Robot();   // instantiated the Robot method by using new and made a call to its constructor
        // Once we have the Robot, we can use the . operator to access its methods
        myRobo.travel();   // when we run this we get back the maxSpeed of the Robot

    }
}
